# Neon / Vercel Postgres connection string
DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require

# Auth (single-user password gate — set in step 3)
AUTH_SECRET=

# Optional: email notifications via Resend
RESEND_API_KEY=
NOTIFICATION_EMAIL=
